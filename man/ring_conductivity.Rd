% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ring_conductivity.R
\name{ring_conductivity}
\alias{ring_conductivity}
\title{Estimate saturated hydraulic conductivity from ponded ring infiltration}
\usage{
ring_conductivity(data, time, volume, radius, workers = 1L)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}. If grouped, the model is fitted independently per
group.}

\item{time}{Bare column name; elapsed time in seconds.}

\item{volume}{Bare column name; water volume in the reservoir (mL),
decreasing as water infiltrates.}

\item{radius}{Ring radius in cm. No default — ring sizes vary and a silent
wrong value would produce incorrect Kfs estimates.}

\item{workers}{Number of parallel workers passed to
\code{\link[=fit_infiltration_horton]{fit_infiltration_horton()}}. Defaults to \code{1}. Values > 1 use
\code{parallel::mclapply()} (Unix only).}
}
\value{
A tibble with one row per group containing:
\itemize{
\item Group keys (if grouped)
\item \code{.fc} — final/steady-state infiltration rate ≈ Kfs (cm/s)
\item \code{.f0} — initial infiltration rate (cm/s)
\item \code{.k} — exponential decay constant (1/s)
\item \code{.fc_std_error}, \code{.f0_std_error}, \code{.k_std_error}
\item \code{.convergence} — logical; \code{TRUE} if NLS converged
}
}
\description{
A convenience wrapper that combines \code{\link[=infiltration_cumulative]{infiltration_cumulative()}},
\code{\link[=infiltration_rate]{infiltration_rate()}}, and \code{\link[=fit_infiltration_horton]{fit_infiltration_horton()}} into a single
pipeline step. Takes raw time–volume readings from a ponded ring
infiltrometer and returns Horton (1940) model parameters, where \code{.fc}
(the asymptotic final rate) is an estimate of \strong{field-saturated hydraulic
conductivity Kfs}.
}
\details{
The Horton model is:
\deqn{f(t) = f_c + (f_0 - f_c) \cdot e^{-k t}}

where \eqn{f_c} ≈ Kfs (cm/s), \eqn{f_0} is the initial infiltration rate,
and \eqn{k} (1/s) is the exponential decay constant.

\strong{When to use the step-by-step approach instead:} If you want to visualise
the raw cumulative infiltration curve or the rate decay before fitting, call
\code{\link[=infiltration_cumulative]{infiltration_cumulative()}}, \code{\link[=infiltration_rate]{infiltration_rate()}}, and
\code{\link[=fit_infiltration_horton]{fit_infiltration_horton()}} individually.
}
\examples{
library(tibble)
library(dplyr)

# Single ring — 100 cm ring radius, 1-hour run
ring <- tibble(
  time   = seq(0, 3600, 360),
  volume = c(500, 440, 390, 355, 328, 308, 292, 280, 271, 265, 260)
)

ring_conductivity(ring, time = time, volume = volume, radius = 10)

# Multi-site: one group_by feeds the full pipeline
multi <- tibble(
  site   = rep(c("A", "B"), each = 11),
  time   = rep(seq(0, 3600, 360), 2),
  volume = c(
    500, 440, 390, 355, 328, 308, 292, 280, 271, 265, 260,
    500, 450, 410, 375, 348, 328, 313, 302, 294, 288, 284
  )
)

multi |>
  group_by(site) |>
  ring_conductivity(time = time, volume = volume, radius = 10)

}
\references{
Horton, R. E. (1940). An approach toward a physical interpretation of
infiltration capacity. \emph{Soil Science Society of America Proceedings},
5, 399–417.
}
\seealso{
\code{\link[=fit_infiltration_horton]{fit_infiltration_horton()}}, \code{\link[=infiltration_cumulative]{infiltration_cumulative()}},
\code{\link[=infiltration_rate]{infiltration_rate()}}, \code{\link[=minidisk_conductivity]{minidisk_conductivity()}}
}
