% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infiltration_rate.R
\name{infiltration_rate}
\alias{infiltration_rate}
\title{Compute interval infiltration rates from cumulative data}
\usage{
infiltration_rate(data, time_col, infiltration_col)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{time_col}{Bare column name; elapsed time in seconds. For BeerKan data,
use \code{.cumulative_time}.}

\item{infiltration_col}{Bare column name; cumulative infiltration depth (cm).
Typically \code{.infiltration} from \code{\link[=infiltration_cumulative]{infiltration_cumulative()}} or
\code{\link[=beerkan_cumulative]{beerkan_cumulative()}}.}
}
\value{
The input \code{data} as a tibble with two additional columns:
\itemize{
\item \code{.rate} — mean infiltration rate over the interval (cm/s); \code{NA} for the
first row within each group
\item \code{.time_mid} — midpoint time of the interval (s); \code{NA} for the first row
}
}
\description{
Computes the mean infiltration rate over each consecutive time interval:
\deqn{f_i = \frac{I_i - I_{i-1}}{t_i - t_{i-1}}}
}
\details{
The rate is assigned to the midpoint time of each interval. The first row
within each group has no preceding observation and returns \code{NA} for both
\code{.rate} and \code{.time_mid}.

The function is \strong{group-aware}: if \code{data} is grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}, differences are computed independently within each
group using \code{dplyr::lag()}.
}
\examples{
library(tibble)
library(dplyr)

dat <- tibble(
  time   = seq(0, 300, 30),
  volume = c(95, 89, 86, 83, 80, 77, 74, 73, 71, 69, 67)
) |>
  infiltration_cumulative(time = time, volume = volume)

infiltration_rate(dat, time_col = time, infiltration_col = .infiltration)

}
