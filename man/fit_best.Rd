% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_best.R
\name{fit_best}
\alias{fit_best}
\title{Fit the BEST algorithm to BeerKan cumulative infiltration data}
\usage{
fit_best(
  data,
  infiltration_col,
  time_col,
  theta_s,
  theta_i,
  n,
  method = c("steady", "slope", "intercept"),
  beta = 0.6,
  gamma = 0.75,
  steady_n = 4L,
  workers = 1L
)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}.}

\item{infiltration_col}{Bare column name of cumulative infiltration depth
(cm). Typically \code{.infiltration} from \code{\link[=beerkan_cumulative]{beerkan_cumulative()}}.}

\item{time_col}{Bare column name of cumulative infiltration time (s).
Typically \code{.cumulative_time} from \code{\link[=beerkan_cumulative]{beerkan_cumulative()}}.}

\item{theta_s}{Saturated volumetric water content (m³/m³ or cm³/cm³). Bare
column name or scalar.}

\item{theta_i}{Initial volumetric water content (m³/m³ or cm³/cm³). Bare
column name or scalar. Must be < \code{theta_s}.}

\item{n}{Van Genuchten shape parameter (dimensionless, must be > 1). Bare
column name or scalar. Typically obtained from \code{\link[=infiltration_vg_params]{infiltration_vg_params()}}
or supplied directly from particle-size analysis.}

\item{method}{Fitting method: \code{"steady"} (default), \code{"slope"}, or
\code{"intercept"}. See the BEST algorithm section above.}

\item{beta}{Haverkamp β integral shape parameter (default \code{0.6}). Standard
value from Haverkamp et al. (1994); override with calibrated values.}

\item{gamma}{Haverkamp γ proportionality constant for 3D lateral flow
correction (default \code{0.75}). Not used in the current 1D implementation
but retained for future 3D extension.}

\item{steady_n}{Number of terminal data points used to define the
steady-state window for \code{method = "steady"} and \code{method = "intercept"}.
Default \code{4} follows the R-algorithm recommendation.}

\item{workers}{Number of parallel workers (default \code{1}). Values > 1 use
\code{\link[parallel:mclapply]{parallel::mclapply()}} on Unix; silently reduced to \code{1} on Windows.}
}
\value{
A tibble with one row per group containing:
\itemize{
\item Group keys (if grouped)
\item \code{.Ks} — saturated hydraulic conductivity (cm/s)
\item \code{.S} — sorptivity (cm/s^0.5)
\item \code{.alpha} — Van Genuchten α (1/cm)
\item \code{.Ks_std_error}, \code{.S_std_error} — standard errors propagated from
the steady-state linear regression
\item \code{.steady_n} — number of points used for steady-state estimation
\item \code{.convergence} — \code{TRUE} if all estimates are finite and positive
}
}
\description{
Implements the Beerkan Estimation of Soil Transfer parameters (BEST)
algorithm (Lassabatère et al., 2006) to estimate saturated hydraulic
conductivity Ks, sorptivity S, and the Van Genuchten scale parameter α from
BeerKan cumulative infiltration data.
}
\note{
θᵣ is assumed to be zero in the Sᵢ calculation (θᵢ / θₛ), which is the
standard BEST simplification. If you have a θᵣ estimate, pass
\code{theta_i = theta_i - theta_r} as an adjusted initial saturation.
}
\section{BEST algorithm}{


BEST uses the two-term approximation of the Haverkamp et al. (1994)
quasi-exact implicit (QEI) infiltration model. At late time (steady state)
the cumulative curve is linear:
\deqn{I(t) \approx K_s t + b}
where the intercept \eqn{b} encodes the sorptivity:
\deqn{b = \frac{S^2}{2 K_s \beta (1 - S_i)}, \quad S_i = \theta_i / \theta_s}

Three fitting methods are available:
\itemize{
\item \strong{\code{"steady"}} (default, recommended): Detects the steady-state portion as
the last \code{steady_n} points (default 4; following the R-algorithm of
Haverkamp et al.). Linear regression on that subset gives Ks (slope) and
b (intercept), then S is back-calculated.
\item \strong{\code{"slope"}}: Estimates S from the early-time transient (slope of I vs
√t on all points before the last \code{steady_n}), then Ks from the late-time
intercept b.
\item \strong{\code{"intercept"}}: Linear regression on the full steady-state subset for
both slope (→ Ks) and intercept (→ b → S), identical to \code{"steady"} but
uses the earliest possible steady-state onset (first point where the rate
change falls below \code{rate_tol}).
}

After estimating Ks and S, the Van Genuchten α is recovered:
\deqn{\alpha = \frac{2 (\theta_s - \theta_i) K_s}{c_p \cdot S^2}}
where \eqn{c_p(n) = \bigl[(2m+1) \cdot B(m+\tfrac{1}{2},\tfrac{1}{2})\bigr]^{-1}}
and \eqn{m = 1 - 1/n}. This is evaluated via base R's \code{beta()} function —
no external dependencies, fully vectorised.
}

\examples{
library(tibble)
library(dplyr)

# Single BeerKan run
run <- tibble(
  pour   = 1:10,
  volume = 100,
  time   = c(12, 18, 25, 30, 33, 34, 35, 35, 36, 35)
) |>
  beerkan_cumulative(volume_col = volume, time_col = time, radius = 5)

fit_best(run,
         infiltration_col = .infiltration,
         time_col         = .cumulative_time,
         theta_s          = 0.45,
         theta_i          = 0.12,
         n                = 1.56)

# Multiple sites via group_by; n from texture lookup
meta <- tibble(
  site    = c("S1", "S2"),
  texture = c("loam", "sandy loam"),
  theta_s = c(0.45, 0.40),
  theta_i = c(0.12, 0.08)
) |>
  infiltration_vg_params(texture = texture, suction = 0)

}
\references{
Lassabatère, L., Angulo-Jaramillo, R., Soria Ugalde, J. M., Cuenca, R.,
Braud, I., & Haverkamp, R. (2006). Beerkan estimation of soil transfer
parameters through infiltration experiments—BEST. \emph{Soil Science Society of
America Journal}, 70(2), 521–532.
\url{https://doi.org/10.2136/sssaj2005.0026}

Haverkamp, R., Ross, P. J., Smettem, K. R. J., & Parlange, J.-Y. (1994).
Three-dimensional analysis of infiltration from the disc infiltrometer:
2. Physically based infiltration equation. \emph{Water Resources Research},
30(11), 2931–2935.
}
