% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_infiltration.R
\name{fit_infiltration}
\alias{fit_infiltration}
\title{Fit the Philip two-term infiltration model}
\usage{
fit_infiltration(data, infiltration_col, sqrt_time_col, workers = 1L)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}.}

\item{infiltration_col}{Bare column name of cumulative infiltration depth
(cm). Typically \code{.infiltration} from \code{\link[=infiltration_cumulative]{infiltration_cumulative()}} or
\code{\link[=beerkan_cumulative]{beerkan_cumulative()}}.}

\item{sqrt_time_col}{Bare column name of the square root of elapsed time
(s^0.5). Typically \code{.sqrt_time}.}

\item{workers}{Number of parallel workers. Defaults to \code{1} (sequential).
Values > 1 use \code{\link[parallel:mclapply]{parallel::mclapply()}} on Unix; silently reduced to \code{1} on
Windows.}
}
\value{
A tibble with one row per group (or one row for ungrouped data)
containing:
\itemize{
\item Group keys (if input was grouped)
\item \code{.C1} — quadratic coefficient (cm/s), the time-proportional term
\item \code{.C2} — linear coefficient (cm/s^0.5), the sorptivity term
\item \code{.C1_std_error}, \code{.C2_std_error} — standard errors
\item \code{.convergence} — \code{TRUE} if \code{lm()} succeeded
}
}
\description{
Fits the Philip (1957) two-term polynomial to cumulative infiltration data to
extract the hydraulic conductivity proxy C₁ and sorptivity proxy C₂:
\deqn{I = C_2 \sqrt{t} + C_1 t}
}
\details{
The fit uses ordinary least squares on the linearised form
\code{I ~ sqrt_time + I(sqrt_time^2)} (with intercept), matching published
methodology (infiltrodiscR, Zhang 1997 tables).

\strong{Interpretation by mode:}
\itemize{
\item \strong{Minidisk}: C₁ feeds into \code{\link[=hydraulic_conductivity_minidisk]{hydraulic_conductivity_minidisk()}} via
\verb{K(h) = C₁ / A}.
\item \strong{Ring (ponded)}: C₁ ≈ Ksat at steady state.
\item \strong{BeerKan}: C₁ provides a quick Philip estimate before running the full
\code{\link[=fit_best]{fit_best()}} algorithm.
}

If \code{data} is grouped with \code{\link[dplyr:group_by]{dplyr::group_by()}}, the model is fitted
independently for each group, returning one row per group in the result.
Parallel fitting across groups is available on Unix-like systems via
\code{workers > 1}.
}
\examples{
library(tibble)
library(dplyr)

dat <- tibble(
  time   = seq(0, 300, 30),
  volume = c(95, 89, 86, 83, 80, 77, 74, 73, 71, 69, 67)
) |>
  infiltration_cumulative(time = time, volume = volume)

fit_infiltration(dat,
                 infiltration_col = .infiltration,
                 sqrt_time_col    = .sqrt_time)

# Grouped: one row per sample
multi <- tibble(
  sample = rep(c("A", "B"), each = 11),
  time   = rep(seq(0, 300, 30), 2),
  volume = c(95, 89, 86, 83, 80, 77, 74, 73, 71, 69, 67,
             83, 77, 64, 61, 58, 45, 42, 35, 29, 17, 15)
) |>
  group_by(sample) |>
  infiltration_cumulative(time = time, volume = volume)

fit_infiltration(multi,
                 infiltration_col = .infiltration,
                 sqrt_time_col    = .sqrt_time)

}
\references{
Philip, J. R. (1957). The theory of infiltration: 4. Sorptivity and algebraic
infiltration equations. \emph{Soil Science}, 84(3), 257–264.

Zhang, R. (1997). Determination of soil sorptivity and hydraulic conductivity
from the disk infiltrometer. \emph{Soil Science Society of America Journal},
61(4), 1024–1030. \url{https://doi.org/10.2136/sssaj1997.03615995006100060008x}
}
