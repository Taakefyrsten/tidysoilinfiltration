% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_infiltration_kostiakov.R
\name{fit_infiltration_kostiakov}
\alias{fit_infiltration_kostiakov}
\title{Fit the Kostiakov (1932) cumulative infiltration model}
\usage{
fit_infiltration_kostiakov(data, infiltration_col, time_col, workers = 1L)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped.}

\item{infiltration_col}{Bare column name of cumulative infiltration depth
(cm). Typically \code{.infiltration} from \code{\link[=infiltration_cumulative]{infiltration_cumulative()}}.}

\item{time_col}{Bare column name of elapsed time (s). Use the original
\code{time} column (not \code{.sqrt_time}).}

\item{workers}{Number of parallel workers (default 1).}
}
\value{
A tibble with one row per group containing:
\itemize{
\item Group keys (if grouped)
\item \code{.a} — Kostiakov coefficient (cm/s^b)
\item \code{.b} — Kostiakov exponent (dimensionless)
\item \code{.a_std_error}, \code{.b_std_error}
\item \code{.convergence}
}
}
\description{
Fits the Kostiakov power model to cumulative infiltration data:
\deqn{I(t) = a \cdot t^b \quad (a > 0,\ 0 < b < 1)}
}
\details{
The model is linearised as \eqn{\log I = \log a + b \log t} to derive
starting values, then refined with \code{nls()} on the original scale.

The instantaneous infiltration rate corresponding to this model is
\eqn{f(t) = a \cdot b \cdot t^{b-1}}, which decreases monotonically with
time when \eqn{0 < b < 1}. Note that the Kostiakov model does not approach
a finite steady-state value (\eqn{f \to 0} as \eqn{t \to \infty}), so it
should not be used to estimate Ksat.

The first row within each group typically has \code{t = 0}, which would produce
a zero or undefined logarithm. Rows with \code{time == 0} or \code{infiltration == 0}
are automatically excluded from fitting.

If \code{data} is grouped, the model is fitted independently per group.
}
\examples{
library(tibble)
library(dplyr)

dat <- tibble(
  time   = seq(0, 3600, 360),
  volume = c(500, 440, 390, 355, 328, 308, 292, 280, 271, 265, 260)
) |>
  infiltration_cumulative(time = time, volume = volume, radius = 10)

fit_infiltration_kostiakov(dat,
                           infiltration_col = .infiltration,
                           time_col         = time)

}
\references{
Kostiakov, A. N. (1932). On the dynamics of the coefficient of
water-percolation in soils. \emph{Transactions of the 6th Commission of the
International Society of Soil Science}, Part A, 17–21.
}
