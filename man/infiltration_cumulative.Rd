% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infiltration_cumulative.R
\name{infiltration_cumulative}
\alias{infiltration_cumulative}
\title{Compute cumulative infiltration from time-volume series}
\usage{
infiltration_cumulative(data, time, volume, radius = 2.25)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{time}{Bare column name or scalar numeric; elapsed time in seconds.}

\item{volume}{Bare column name or scalar numeric; water volume in the
reservoir (mL), decreasing as water infiltrates.}

\item{radius}{Disc or ring radius in cm. Defaults to \code{2.25}, the standard
Minidisk Infiltrometer radius. Pass the actual ring radius for standard
ring infiltrometers.}
}
\value{
The input \code{data} as a tibble with three additional columns:
\itemize{
\item \code{.sqrt_time} — square root of elapsed time (s^0.5)
\item \code{.volume_infiltrated} — cumulative volume infiltrated from the start of
the run (mL)
\item \code{.infiltration} — cumulative infiltration depth (cm)
}
}
\description{
Converts raw time–volume readings from a Minidisk or ring infiltrometer into
cumulative infiltration depth and its square root of time, ready for
downstream model fitting.
}
\details{
Cumulative infiltration is calculated as:
\deqn{I = \frac{V_0 - V}{\pi r^2}}
where \eqn{V_0} is the initial volume (first observation within each group),
\eqn{V} is the volume at time \eqn{t}, and \eqn{r} is the disc or ring
radius.

The function is \strong{group-aware}: if \code{data} is grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}, the initial volume \eqn{V_0} is taken as the first
observation \emph{within each group}, so multiple samples can be processed in a
single pipeline step.
}
\examples{
library(tibble)
library(dplyr)

# Single sample
minidisk <- tibble(
  time   = seq(0, 300, 30),
  volume = c(95, 89, 86, 83, 80, 77, 74, 73, 71, 69, 67)
)
infiltration_cumulative(minidisk, time = time, volume = volume)

# Multiple samples via group_by — initial volume taken per group
multi <- tibble(
  sample = rep(c("A", "B"), each = 11),
  time   = rep(seq(0, 300, 30), 2),
  volume = c(95, 89, 86, 83, 80, 77, 74, 73, 71, 69, 67,
             83, 77, 64, 61, 58, 45, 42, 35, 29, 17, 15)
)
multi |>
  group_by(sample) |>
  infiltration_cumulative(time = time, volume = volume)

# Ring infiltrometer with 10 cm radius
ring <- tibble(time = seq(0, 600, 60), volume = 500 - seq(0, 200, 20))
infiltration_cumulative(ring, time = time, volume = volume, radius = 10)

}
\references{
Philip, J. R. (1957). The theory of infiltration: 4. Sorptivity and algebraic
infiltration equations. \emph{Soil Science}, 84(3), 257–264.
}
