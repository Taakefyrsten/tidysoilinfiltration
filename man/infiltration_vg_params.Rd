% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infiltration_vg_params.R
\name{infiltration_vg_params}
\alias{infiltration_vg_params}
\title{Look up Van Genuchten parameters from the Minidisk reference table}
\usage{
infiltration_vg_params(data, texture, suction)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{texture}{Bare column name or character scalar; USDA texture class
(lowercase, e.g. \code{"clay loam"}). Must match one of the 12 classes in
\link{minidisk_vg_params}.}

\item{suction}{Bare column name, numeric scalar, or character scalar
(e.g. \code{"2cm"}); suction level in cm. Must match one of 0.5, 1, 2, 3, 4,
5, 6, or 7 cm for full lookup. Pass \code{0} to retrieve only shape parameters.}
}
\value{
The input \code{data} as a tibble with three additional columns:
\itemize{
\item \code{.n} — Van Genuchten n parameter (dimensionless, > 1)
\item \code{.alpha} — Van Genuchten α parameter (1/cm)
\item \code{.A} — tabulated A value for a 2.25 cm disc; \code{NA} when suction is 0
}
}
\description{
Appends Van Genuchten shape parameters (n, α) and the tabulated A value to
each row of \code{data} by matching texture class and suction level against the
built-in \link{minidisk_vg_params} lookup table (Decagon Devices, Inc., 2005).
}
\details{
The lookup table covers 12 USDA texture classes and suction levels of 0.5,
1, 2, 3, 4, 5, 6, and 7 cm (all for a disc radius of 2.25 cm). For the
BeerKan experiment (null pressure head), pass \code{suction = 0} — the function
will return the tabulated shape parameters (n, α) while setting \code{.A} to
\code{NA}, as A is not defined at zero suction in the Decagon table.
}
\examples{
library(tibble)

soils <- tibble(
  site    = c("A", "B"),
  texture = c("sandy loam", "clay loam"),
  suction = c(4, 2)
)
infiltration_vg_params(soils, texture = texture, suction = suction)

# Scalar suction
infiltration_vg_params(soils, texture = texture, suction = 3)

# Character suction string
soils2 <- tibble(texture = "loam", suction = "2cm")
infiltration_vg_params(soils2, texture = texture, suction = suction)

# For BeerKan: retrieve shape params only (A is NA at suction = 0)
beerkan_meta <- tibble(texture = "sandy loam", theta_s = 0.45, theta_i = 0.1)
infiltration_vg_params(beerkan_meta, texture = texture, suction = 0)

}
\references{
Decagon Devices, Inc. (2005). \emph{Mini Disk Infiltrometer User's Manual}.
}
\seealso{
\link{minidisk_vg_params} for the full lookup table,
\code{\link[=parameter_A_zhang]{parameter_A_zhang()}} to compute A analytically from n and α.
}
