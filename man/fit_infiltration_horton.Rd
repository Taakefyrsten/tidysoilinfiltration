% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_infiltration_horton.R
\name{fit_infiltration_horton}
\alias{fit_infiltration_horton}
\title{Fit the Horton (1940) infiltration rate model}
\usage{
fit_infiltration_horton(data, rate_col, time_col, workers = 1L)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped.}

\item{rate_col}{Bare column name of infiltration rates (cm/s). Typically
\code{.rate} from \code{\link[=infiltration_rate]{infiltration_rate()}}.}

\item{time_col}{Bare column name of midpoint times (s). Typically
\code{.time_mid} from \code{\link[=infiltration_rate]{infiltration_rate()}}.}

\item{workers}{Number of parallel workers (default 1).}
}
\value{
A tibble with one row per group containing:
\itemize{
\item Group keys (if grouped)
\item \code{.fc} — final/steady-state rate ≈ Ksat (cm/s)
\item \code{.f0} — initial rate (cm/s)
\item \code{.k} — decay constant (1/s)
\item \code{.fc_std_error}, \code{.f0_std_error}, \code{.k_std_error}
\item \code{.convergence}
}
}
\description{
Fits the Horton (1940) exponential decay model to infiltration rates
computed at discrete time intervals:
\deqn{f(t) = f_c + (f_0 - f_c) \cdot e^{-k t}}
}
\details{
where \eqn{f_c} (cm/s) is the final/steady-state rate (≈ Ksat for ponded
ring experiments), \eqn{f_0} (cm/s) is the initial rate, and \eqn{k} (1/s)
is the decay constant.

Inputs are typically interval rates computed with \code{\link[=infiltration_rate]{infiltration_rate()}}:
\code{.rate} (y) and \code{.time_mid} (x). The first row within each group has
\code{NA} values and is automatically excluded from fitting.

Starting values are derived automatically from the data range:
\itemize{
\item \code{f0_start} = maximum observed rate
\item \code{fc_start} = minimum observed rate
\item \code{k_start} = 0.01 (1/s)
}

If \code{data} is grouped, the model is fitted independently per group. Parallel
fitting is available on Unix via \code{workers > 1}.
}
\examples{
library(tibble)
library(dplyr)

dat <- tibble(
  time   = seq(0, 3600, 360),
  volume = c(500, 440, 390, 355, 328, 308, 292, 280, 271, 265, 260)
) |>
  infiltration_cumulative(time = time, volume = volume, radius = 10) |>
  infiltration_rate(time_col = time, infiltration_col = .infiltration)

fit_infiltration_horton(dat, rate_col = .rate, time_col = .time_mid)

}
\references{
Horton, R. E. (1940). An approach toward a physical interpretation of
infiltration capacity. \emph{Soil Science Society of America Proceedings},
5, 399–417.
}
